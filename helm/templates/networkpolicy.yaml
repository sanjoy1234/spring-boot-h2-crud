apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: spring-boot-h2-crud
spec:
  podSelector:
    matchLabels: {app: spring-boot-h2-crud}
  policyTypes: [Ingress, Egress]
  ingress:
  - ports:
    - protocol: TCP
      port: 8080
  egress:
  - to:
    - namespaceSelector: {}
